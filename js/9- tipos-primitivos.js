let numero = 2; // number
let nome = 'Will'; //string
let indefinido = undefined; // não aponta para nenhum local na memória
let nulo = null; // não aponta para nenhum local na memória
let boleano = true; // ou false // boleano é um valor logico como verdadeiro ou falso  


console.log(typeof numero, numero);
console.log(typeof nome, nome);
console.log(typeof indefinido, indefinido);
console.log(typeof nulo, nulo); // nulo é descrito como objeto mas não é um objeto
console.log(typeof boleano, boleano);

// Diferença entre nulo e undefined é que em undefined é o próprio js que deixa o valor undefine já o nulo podemos definir uma váriavel com valor nulo 